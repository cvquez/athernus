

<% model_name = t('activerecord.models.category.one', default: 'Category') %>
<% title = "#{model_name}" %>
<% title_sm = "Última modificación: #{strftime @category.updated_at}" %>
<% content_for :title, title %>
<% content_for :page_title do %>
<%= title %>
<% end %>
<% content_for :page_title_small, title_sm %>
<% content_for :page_actions do %>
<a href="<%= categories_path %>" class="btn btn-white"><%= feather 'arrow-left' %> Volver a la lista</a>
<a href="<%= edit_category_url(@category) %>" class="btn btn-primary"><%= feather 'edit-2' %> Editar</a>
<% end %>

<table class="table table-striped table-responsive">
  <tbody>
      <tr>
      <th><%= t('activerecord.attributes.category.name') %></th>
      <td>
                  <%=  @category.name %>
              </td>
    </tr>
    </tbody>
</table>
