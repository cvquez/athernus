

<% model_name = t('activerecord.models.person.one', default: 'Person') %>
<% title = "#{model_name}" %>
<% title_sm = "Última modificación: #{strftime @person.updated_at}" %>
<% content_for :title, title %>
<% content_for :page_title do %>
<%= title %>
<% end %>
<% content_for :page_title_small, title_sm %>
<% content_for :page_actions do %>
<a href="<%= people_path %>" class="btn btn-white"><%= feather 'arrow-left' %> Volver a la lista</a>
<a href="<%= edit_person_url(@person) %>" class="btn btn-primary"><%= feather 'edit-2' %> Editar</a>
<% end %>

<table class="table table-striped table-responsive">
  <tbody>
      <tr>
      <th><%= t('activerecord.attributes.person.name') %></th>
      <td>
                  <%=  @person.name %>
              </td>
    </tr>
      <tr>
      <th><%= t('activerecord.attributes.person.email') %></th>
      <td>
                  <%=  @person.email %>
              </td>
    </tr>
    </tbody>
</table>
