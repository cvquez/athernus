<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= render 'application/favicon' %>
  <title><%= page_title content_for(:title) %> </title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application' %>
  <%= javascript_include_tag 'application', "data-turbo-track": "reload" %>
  <link href="<%= asset_path('/web/css/styles.css?v=2.2') %>" rel="stylesheet"/>
  <link rel="stylesheet" href="<%= asset_path('/style.css?v=2.2') %>">
  <%= content_for :head_scripts %>
  <%= render 'shared/google_tag_manager' %>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.min.css">
</head>
<body class="<%= content_for?(:body_class) ? content_for(:body_class) : 'default-layout' %>">

<%= render 'shared/site/header' %>
<%= yield %>
<%= render 'shared/site/footer' %>

<%= content_for :scripts %>
<%= render 'shared/toast' %>
</body>
</html>