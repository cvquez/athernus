

<% model_name = t('activerecord.models.answer.one', default: 'Answer') %>
<% title = "#{model_name}" %>
<% title_sm = "Última modificación: #{strftime @answer.updated_at}" %>
<% content_for :title, title %>
<% content_for :page_title do %>
<%= title %>
<% end %>
<% content_for :page_title_small, title_sm %>
<% content_for :page_actions do %>
<a href="<%= answers_path %>" class="btn btn-white"><%= feather 'arrow-left' %> Volver a la lista</a>
<a href="<%= edit_answer_url(@answer) %>" class="btn btn-primary"><%= feather 'edit-2' %> Editar</a>
<% end %>

<table class="table table-striped table-responsive">
  <tbody>
      <tr>
      <th><%= t('activerecord.attributes.answer.value') %></th>
      <td>
                  <%=  @answer.value %>
              </td>
    </tr>
      <tr>
      <th><%= t('activerecord.attributes.answer.response_id') %></th>
      <td>
                  <%=  @answer.response_id %>
              </td>
    </tr>
      <tr>
      <th><%= t('activerecord.attributes.answer.question_id') %></th>
      <td>
                  <%=  @answer.question_id %>
              </td>
    </tr>
    </tbody>
</table>
