

<% model_name = t('activerecord.models.response.one', default: 'Response') %>
<% title = "#{model_name}" %>
<% title_sm = "Última modificación: #{strftime @response.updated_at}" %>
<% content_for :title, title %>
<% content_for :page_title do %>
<%= title %>
<% end %>
<% content_for :page_title_small, title_sm %>
<% content_for :page_actions do %>
<a href="<%= responses_path %>" class="btn btn-white"><%= feather 'arrow-left' %> Volver a la lista</a>
<a href="<%= edit_response_url(@response) %>" class="btn btn-primary"><%= feather 'edit-2' %> Editar</a>
<% end %>

<table class="table table-striped table-responsive">
  <tbody>
      <tr>
      <th><%= t('activerecord.attributes.response.questionnaire_id') %></th>
      <td>
                  <%=  @response.questionnaire_id %>
              </td>
    </tr>
      <tr>
      <th><%= t('activerecord.attributes.response.person_id') %></th>
      <td>
                  <%=  @response.person_id %>
              </td>
    </tr>
    </tbody>
</table>
