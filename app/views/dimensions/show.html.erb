

<% model_name = t('activerecord.models.dimension.one', default: 'Dimension') %>
<% title = "#{model_name}" %>
<% title_sm = "Última modificación: #{strftime @dimension.updated_at}" %>
<% content_for :title, title %>
<% content_for :page_title do %>
<%= title %>
<% end %>
<% content_for :page_title_small, title_sm %>
<% content_for :page_actions do %>
<a href="<%= dimensions_path %>" class="btn btn-white"><%= feather 'arrow-left' %> Volver a la lista</a>
<a href="<%= edit_dimension_url(@dimension) %>" class="btn btn-primary"><%= feather 'edit-2' %> Editar</a>
<% end %>

<table class="table table-striped table-responsive">
  <tbody>
      <tr>
      <th><%= t('activerecord.attributes.dimension.name') %></th>
      <td>
                  <%=  @dimension.name %>
              </td>
    </tr>
    </tbody>
</table>
