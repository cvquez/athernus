
<% model_name = t('activerecord.models.dimension.one', default: 'Dimension') %>
<% title = @dimension.persisted? ? "Editar #{model_name}" : "Nuevo #{model_name}" %>
<% title_sm = @dimension.persisted? ? "Última modificación: #{strftime @dimension.updated_at}" : '' %>
<% content_for :title, title %>
<% content_for :page_title, title %>
<% content_for :page_title_small, title_sm %>
<% content_for :page_actions do %>
  <%= link_to dimensions_path, class: 'btn btn-white' do %>
    <%= feather 'arrow-left' %> Volver a la lista
  <% end  %>
  <a href="#submit" class="btn btn-primary"><%= feather :plus %> Guardar</a>
<% end %>

<%= simple_form_for(@dimension) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="row">
    <div class="col-md-6">
      <%= f.input :name %>
    </div>
    <div class="col-md-6">
      <%= f.input :color, as: :color %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class: 'btn btn-primary' %>
    <%= link_to 'Ver registro', @dimension, class: 'btn btn-secondary' if @dimension.id %>
    <%= link_to 'Volver', dimensions_path, class: 'btn btn-secondary' %>
  </div>
<% end %>
