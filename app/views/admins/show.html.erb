<% model_name = t('activerecord.models.admin.one', default: 'Admin') %>
<% title = "#{model_name}" %>
<% title_sm = "Última modificación: #{strftime @admin.updated_at}" %>
<% content_for :title, title %>
<% content_for :page_title do %>
  <%= model_name %>
<% end %>
<% content_for :page_title_small, title_sm %>
<% content_for :page_actions do %>
  <a href="<%= admins_path %>" class="btn btn-white"><%= feather 'arrow-left' %> Volver a la lista</a>
  <a href="<%= edit_admin_url(@admin) %>" class="btn btn-primary"><%= feather 'edit-2' %> Editar</a>
<% end %>

<table class="table table-striped table-responsive">
  <tbody>
  <tr>
    <th><%= t('activerecord.attributes.admin.name') %></th>
    <td>
      <%= @admin.name %>
    </td>
  </tr>
  <tr>
    <th><%= t('activerecord.attributes.admin.email') %></th>
    <td>
      <%= @admin.email %>
    </td>
  </tr>
  <tr>
    <th><%= t('activerecord.attributes.admin.role_id') %></th>
    <td>
      <%= @admin.role_id %>
    </td>
  </tr>
  <tr>
    <th><%= t('activerecord.attributes.admin.active') %></th>
    <td>
      <%= span_status @admin.active %>
    </td>
  </tr>
  </tbody>
</table>
