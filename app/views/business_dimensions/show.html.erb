

<% model_name = t('activerecord.models.business_dimension.one', default: 'Business dimension') %>
<% title = "#{model_name}" %>
<% title_sm = "Última modificación: #{strftime @business_dimension.updated_at}" %>
<% content_for :title, title %>
<% content_for :page_title do %>
<%= title %>
<% end %>
<% content_for :page_title_small, title_sm %>
<% content_for :page_actions do %>
<a href="<%= business_dimensions_path %>" class="btn btn-white"><%= feather 'arrow-left' %> Volver a la lista</a>
<a href="<%= edit_business_dimension_url(@business_dimension) %>" class="btn btn-primary"><%= feather 'edit-2' %> Editar</a>
<% end %>

<table class="table table-striped table-responsive">
  <tbody>
      <tr>
      <th><%= t('activerecord.attributes.business_dimension.name') %></th>
      <td>
                  <%=  @business_dimension.name %>
              </td>
    </tr>
    </tbody>
</table>
