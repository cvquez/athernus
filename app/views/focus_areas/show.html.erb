

<% model_name = t('activerecord.models.focus_area.one', default: 'Focus area') %>
<% title = "#{model_name}" %>
<% title_sm = "Última modificación: #{strftime @focus_area.updated_at}" %>
<% content_for :title, title %>
<% content_for :page_title do %>
<%= title %>
<% end %>
<% content_for :page_title_small, title_sm %>
<% content_for :page_actions do %>
<a href="<%= focus_areas_path %>" class="btn btn-white"><%= feather 'arrow-left' %> Volver a la lista</a>
<a href="<%= edit_focus_area_url(@focus_area) %>" class="btn btn-primary"><%= feather 'edit-2' %> Editar</a>
<% end %>

<table class="table table-striped table-responsive">
  <tbody>
      <tr>
      <th><%= t('activerecord.attributes.focus_area.name') %></th>
      <td>
                  <%=  @focus_area.name %>
              </td>
    </tr>
    </tbody>
</table>
